---
interface Props {
  delay?: number;
  as?: string;
  class?: string;
}
const { delay = 0, as: Tag = "div", class: className = "" } = Astro.props;
---
<Tag class={`reveal ${className}`} style={delay ? `--reveal-delay:${delay}ms` : undefined}>
  <slot />
</Tag>
